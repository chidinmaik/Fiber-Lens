// Re-export from the canonical @fiber/lens-sdk package.
// The SDK's FiberClient includes all the same functionality plus
// static parseErrorCode, nodeInfo(), and full JSDoc documentation.
export { FiberClient } from "@fiber/lens-sdk";
