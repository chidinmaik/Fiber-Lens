// Re-export from the canonical @fiber/lens-sdk package.
// The SDK's DiagnosticEngine is the same pure-logic classifier,
// usable standalone by any project without Prisma or database dependencies.
export { DiagnosticEngine } from "@fiber/lens-sdk";
